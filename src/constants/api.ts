export const api = {
    AUTH_USER: {
        CREATEACCESSCODE: "createAccessCode",
        VALIDATEACCESSCODE: "validateAccessCode"
    }
}

export const querykey = {
     AUTH_USER: {
        CREATEACCESSCODE: "createAccessCode",
        VALIDATEACCESSCODE: "validateAccessCode"
    }
}